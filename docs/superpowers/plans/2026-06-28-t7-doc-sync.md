# T7 Doc Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Align all T7 operational documents with the completed env fixes and the Claude `verdict-B.md` acceptance result.

**Architecture:** Treat the scripts and verdict files as source of truth, then update only docs that future agents will read as runbooks. Keep content wiki pages out of scope; this is a documentation consistency pass plus tracking the already-produced `verdict-B.md`.

**Tech Stack:** Windows PowerShell 5.1, Git, Markdown, existing T7 eval scripts.

---

## Scope And Current Facts

- Worktree: `D:\cursor\小马哥时政历史\.worktrees\t7-quality-gate`
- Branch: `codex/t7-quality-gate`
- Existing untracked file to preserve and commit: `小马哥知识库/eval/verdict-B.md`
- Final env-fix HEAD before this doc-sync plan: `257195e`
- T7 acceptance facts from `verdict-B.md`:
  - Sample A: severe `0`, medium hit rate `0%`, slug `100%`, noise `0%`
  - Sample B: severe `0`, medium hit rate `~2.0% (3/151)`, slug `~100%`, noise `0%`
  - Strict thresholds passed: severe `=0`, medium `<=5%`, slug `>=98%`, noise `<=5%`
  - Release marker: `ENABLE_SECOND_HALF`
  - Non-gating backlog: article `444` needs two policy-claim takes; article `91` needs one prediction take.

## File Structure

| File | Responsibility | Change Type |
|---|---|---|
| `小马哥知识库/eval/verdict-B.md` | Claude authoritative acceptance verdict | Add to git |
| `小马哥知识库/AGENT-OPS.md` | Current agent operating discipline | Correct stale P7 encoding statement |
| `小马哥知识库/eval/claude-adjudication.md` | Live Claude adjudication prompt | Switch generic `verdict.md` to sample-specific output |
| `docs/superpowers/specs/2026-06-28-t7-quality-gate-design.md` | T7 design record | Mark accepted, fix output names, add backlog |
| `docs/superpowers/plans/2026-06-28-t7-quality-gate.md` | Quality-gate runbook | Add regen-index finish steps and sample-specific verdict names |
| `docs/superpowers/plans/2026-06-28-t7-env-fixes.md` | Env-fix plan and script snippets | Sync final helper logic from implementation |

---

## Task 1: Track Verdict-B And Update Acceptance Design Record

**Files:**
- Add: `小马哥知识库/eval/verdict-B.md`
- Modify: `docs/superpowers/specs/2026-06-28-t7-quality-gate-design.md`

- [ ] **Step 1: Verify `verdict-B.md` has the acceptance anchors**

Run from the worktree root:

```powershell
Select-String -Path .\小马哥知识库\eval\verdict-B.md -Pattern '判定:过线|严重: 0|中级命中率: ~2.0%|slug 一致率: ~100%|噪声残留率: 0%|ENABLE_SECOND_HALF|444|91' | Select-Object -ExpandProperty Line
```

Expected: output includes the pass line, all four metric lines, `ENABLE_SECOND_HALF`, and backlog references for `444` and `91`.

- [ ] **Step 2: Update the design flow output name**

In `docs/superpowers/specs/2026-06-28-t7-quality-gate-design.md`, replace:

```markdown
└─[2] Claude 复合 pass ── claude-adjudication.md ──▶ eval/verdict.md
```

with:

```markdown
└─[2] Claude 复合 pass ── claude-adjudication.md ──▶ eval/verdict-A.md / eval/verdict-B.md
```

- [ ] **Step 3: Update the delivery file list**

In the same file, replace:

```markdown
  verdict.md                # Claude 产出(运行后生成)
```

with:

```markdown
  verdict-A.md              # Claude 产出:样本A 调 prompt 裁决
  verdict-B.md              # Claude 产出:样本B 正式验收裁决
```

- [ ] **Step 4: Change the status line**

Replace:

```markdown
Status: 待用户复核
```

with:

```markdown
Status: 样本B 严格档过线(2026-06-28)
```

- [ ] **Step 5: Append the acceptance conclusion**

Append this exact section to the end of `docs/superpowers/specs/2026-06-28-t7-quality-gate-design.md`:

```markdown
## 验收结论(T7)

- 2026-06-28 · 样本A + 样本B 双双过线,严格档四项全达标。
- 样本B 指标:`严重=0`,`中级命中率≈2.0%(3/151)`,`slug 一致率≈100%`,`噪声残留率=0%`。
- 放行后半程:`ENABLE_SECOND_HALF`。下一阶段是 T9:带 embedding 重建 + `gbrain think`。
- 非 gating 返工 backlog:
  - `444`:补两条政策主张 take:韬光养晦失效→主动出击;税制改革。
  - `91`:补一条预测 take:美国战略收缩下“下一个放弃东欧”离间中俄。
- T9 注意事项:DashScope 国内端点;清理或确认 config 中没有 `embedding_disabled` 残留。
```

- [ ] **Step 6: Commit Task 1**

Run:

```powershell
git add .\小马哥知识库\eval\verdict-B.md .\docs\superpowers\specs\2026-06-28-t7-quality-gate-design.md
git commit -m "docs(t7): record sample-B acceptance verdict"
```

Expected: commit succeeds and includes only the verdict plus design record.

---

## Task 2: Sync Live Agent Prompts And Operating Notes

**Files:**
- Modify: `小马哥知识库/AGENT-OPS.md`
- Modify: `小马哥知识库/eval/claude-adjudication.md`

- [ ] **Step 1: Correct the P7 encoding note in `AGENT-OPS.md`**

Replace the current line:

```markdown
- 历史文件 `wiki/log.md`、`.compile-log` 是 GBK 编码,agent 复读前先转 UTF-8(P7)。
```

with:

```markdown
- worktree 内 `wiki/log.md` 已验证为 UTF-8;旧运行日志或主树 ignored `.compile-log` 若遇 GBK,先用 `eval/fix-encoding.ps1 -Path .\.compile-log` 或显式绝对路径转 UTF-8(BOM),不要默认改主树(P7)。
```

- [ ] **Step 2: Make Claude output sample-specific**

In `小马哥知识库/eval/claude-adjudication.md`, replace:

```markdown
## 输出 `eval/verdict.md`
```

with:

```markdown
## 输出样本专属 verdict 文件

- 本轮样本是 `eval/sample-A.txt` 时,只写 `eval/verdict-A.md`。
- 本轮样本是 `eval/sample-B.txt` 时,只写 `eval/verdict-B.md`。
- 不要写通用 `eval/verdict.md`,避免覆盖另一轮裁决。
```

- [ ] **Step 3: Change aggregation wording to one sample**

Replace:

```markdown
跨样本汇总,按下面口径算四个指标:
```

with:

```markdown
对本轮样本汇总,按下面口径算四个指标:
```

- [ ] **Step 4: Update the write-scope rule**

Replace:

```markdown
- 只写 `eval/verdict.md`,**不改** `raw/`、`wiki/`、`aliases.md`。
```

with:

```markdown
- 只写本轮对应的 `eval/verdict-A.md` 或 `eval/verdict-B.md`,**不改** `raw/`、`wiki/`、`aliases.md`。
```

- [ ] **Step 5: Verify live prompt consistency**

Run:

```powershell
Select-String -Path .\小马哥知识库\AGENT-OPS.md -Pattern 'wiki/log.md.*UTF-8|fix-encoding.ps1'
Select-String -Path .\小马哥知识库\eval\claude-adjudication.md -Pattern '输出样本专属 verdict 文件|verdict-A.md|verdict-B.md|本轮样本汇总'
```

Expected: all four prompt/ops concepts appear.

- [ ] **Step 6: Commit Task 2**

Run:

```powershell
git add .\小马哥知识库\AGENT-OPS.md .\小马哥知识库\eval\claude-adjudication.md
git commit -m "docs(t7): align live prompts with sample-specific verdicts"
```

---

## Task 3: Update The Quality-Gate Runbook

**Files:**
- Modify: `docs/superpowers/plans/2026-06-28-t7-quality-gate.md`

- [ ] **Step 1: Remove instructions that imply editing `codex-brief.md` per run**

In Task 3 Step 1, replace:

```markdown
把 `eval/codex-brief.md` 末尾追加一行"本轮样本:`eval/sample-A.txt`",在 KB 根开一个 Codex 会话执行。交互式最省事:
```

with:

```markdown
不要修改 `eval/codex-brief.md` 本体。运行时把 `eval/codex-brief.md` 全文与"本轮样本:`eval/sample-A.txt`"拼成 prompt,在 KB 根开一个 Codex 会话执行。交互式最省事:
```

- [ ] **Step 2: Add regen-index after sample A Codex pass**

After Task 3 Step 2 validation, add this step before the existing status check:

```markdown
- [ ] **Step 3: 重建累计 `_index.json`**

Run:
```powershell
& .\eval\regen-index.ps1
```
Expected: `index rebuilt: count=32 A=8 B=24` when A+B findings are already present; during an A-only rerun, `count` equals the current number of present findings while both sample manifests are still listed.
```

Then renumber the following status and commit steps.

- [ ] **Step 3: Include `_index.json` in the sample A commit**

Replace:

```powershell
git add eval/codex-findings/*.json
git commit -m "eval: codex identify pass on sample-A (round 1)"
```

with:

```powershell
git add eval/codex-findings/*.json eval/codex-findings/_index.json
git commit -m "eval: codex identify pass on sample-A (round 1)"
```

- [ ] **Step 4: Add regen-index after sample B Codex pass**

After Task 6 Step 2 validation, add:

```markdown
- [ ] **Step 3: 重建累计 `_index.json`**

Run:
```powershell
& .\eval\regen-index.ps1
```
Expected: `index rebuilt: count=32 A=8 B=24`.
```

Then renumber the existing commit step.

- [ ] **Step 5: Include `_index.json` in the sample B commit**

Replace:

```powershell
git add eval/codex-findings/*.json
git commit -m "eval: codex identify pass on sample-B (acceptance)"
```

with:

```powershell
git add eval/codex-findings/*.json eval/codex-findings/_index.json
git commit -m "eval: codex identify pass on sample-B (acceptance)"
```

- [ ] **Step 6: Update Claude output wording in Tasks 4 and 7**

Ensure Task 4 says Claude writes `eval/verdict-A.md`, and Task 7 says Claude writes `eval/verdict-B.md`. Keep those file names in the `git add` examples.

- [ ] **Step 7: Add current acceptance and backlog note**

Near Task 8, add:

```markdown
当前状态(2026-06-28):`eval/verdict-B.md` 已判定严格档过线,放行 `ENABLE_SECOND_HALF`。非 gating backlog 只保留 3 个中级 take 补抽:444 两条政策主张 take,91 一条东欧预测 take。
```

- [ ] **Step 8: Verify runbook no longer has stale commands**

Run:

```powershell
Select-String -Path .\docs\superpowers\plans\2026-06-28-t7-quality-gate.md -Pattern '追加一行|git add eval/codex-findings/\*.json$|eval/verdict.md'
Select-String -Path .\docs\superpowers\plans\2026-06-28-t7-quality-gate.md -Pattern 'regen-index.ps1|_index.json|verdict-A.md|verdict-B.md|ENABLE_SECOND_HALF'
```

Expected: first command has no stale bare matches; second command shows the new operational anchors.

- [ ] **Step 9: Commit Task 3**

Run:

```powershell
git add .\docs\superpowers\plans\2026-06-28-t7-quality-gate.md
git commit -m "docs(t7): update quality-gate runbook after acceptance"
```

---

## Task 4: Sync The Env-Fixes Plan With Final Implementation

**Files:**
- Modify: `docs/superpowers/plans/2026-06-28-t7-env-fixes.md`

- [ ] **Step 1: Update the `regen-index.ps1` snippet for numeric JSON files**

Replace the snippet line:

```powershell
$present = @(Get-ChildItem $fd -Filter *.json | Where-Object { $_.BaseName -ne '_index' } | ForEach-Object { [int]$_.BaseName })
```

with:

```powershell
$present = @(Get-ChildItem $fd -Filter *.json | Where-Object { $_.BaseName -match '^\d+$' } | ForEach-Object { [int]$_.BaseName })
```

- [ ] **Step 2: Update the `regen-index.ps1` snippet for LF output**

Replace:

```powershell
$idx | ConvertTo-Json -Depth 5 | Set-Content (Join-Path $fd '_index.json') -Encoding UTF8
```

with:

```powershell
$json = (($idx | ConvertTo-Json -Depth 5) -join "`n") -replace "`r`n", "`n"
[IO.File]::WriteAllText((Join-Path $fd '_index.json'), $json + "`n", (New-Object System.Text.UTF8Encoding($false)))
```

- [ ] **Step 3: Update stale manual-index contrast text**

Replace:

```markdown
- 全部样本处理完,在 `eval/codex-findings/_index.json` 写一行汇总:`{"sample": "A|B", "count": N, "files": [...], "generated_at": "..."}`。
```

with:

```markdown
- 全部样本处理完,不要手写 `_index.json`;必须运行 `eval/regen-index.ps1`,由脚本从现有 findings 和 sample manifests 重建累计索引。
```

- [ ] **Step 4: Update `fix-encoding.ps1` snippet for resolved paths**

After:

```powershell
if(-not (Test-Path -LiteralPath $Path)){ throw "not found: $Path" }
```

add:

```powershell
$resolvedPath = (Resolve-Path -LiteralPath $Path).ProviderPath
```

Then replace:

```powershell
$bytes = [IO.File]::ReadAllBytes($Path)
```

with:

```powershell
$bytes = [IO.File]::ReadAllBytes($resolvedPath)
```

And replace:

```powershell
[IO.File]::WriteAllText($Path, $gb.GetString($bytes), $utf8bom)
```

with:

```powershell
[IO.File]::WriteAllText($resolvedPath, $gb.GetString($bytes), $utf8bom)
```

- [ ] **Step 5: Correct the T5 self-test path**

Replace:

```powershell
& .\eval\fix-encoding.ps1 -Path .\小马哥知识库\wiki\log.md
```

with:

```powershell
& .\eval\fix-encoding.ps1 -Path .\wiki\log.md
```

- [ ] **Step 6: Verify no stale implementation snippets remain**

Run:

```powershell
Select-String -Path .\docs\superpowers\plans\2026-06-28-t7-env-fixes.md -Pattern 'BaseName -ne ''_index''|Set-Content .*_index|ReadAllBytes\(\$Path\)|WriteAllText\(\$Path|写一行汇总'
Select-String -Path .\docs\superpowers\plans\2026-06-28-t7-env-fixes.md -Pattern "BaseName -match '\^\\d\+\$'|Resolve-Path|WriteAllText\(\(Join-Path"
```

Expected: first command has no matches; second command shows the final implementation anchors.

- [ ] **Step 7: Commit Task 4**

Run:

```powershell
git add .\docs\superpowers\plans\2026-06-28-t7-env-fixes.md
git commit -m "docs(t7): sync env-fix plan with final helper behavior"
```

---

## Task 5: Cross-Document Verification

**Files:**
- No expected edits unless verification reveals a missed stale instruction.

- [ ] **Step 1: Search for stale generic verdict output**

Run:

```powershell
rg -n --glob '*.md' --glob '!小马哥知识库/wiki/people/**' --glob '!小马哥知识库/wiki/orgs/**' --glob '!小马哥知识库/wiki/countries/**' --glob '!小马哥知识库/wiki/events/**' --glob '!小马哥知识库/wiki/takes/**' --glob '!小马哥知识库/wiki/sources/**' 'eval/verdict\.md|输出 `eval/verdict\.md`|只写 `eval/verdict\.md`'
```

Expected: no matches.

- [ ] **Step 2: Search for stale index and encoding instructions**

Run:

```powershell
rg -n --glob '*.md' --glob '*.ps1' 'BaseName -ne ''_index''|Set-Content .*_index|ReadAllBytes\(\$Path\)|WriteAllText\(\$Path|写一行汇总|wiki/log.md.*GBK' .
```

Expected: no matches except quoted historical context that explicitly says it is old and not an instruction.

- [ ] **Step 3: Verify live eval artifacts**

Run from `小马哥知识库`:

```powershell
$idx = Get-Content -Raw -Encoding UTF8 -LiteralPath .\eval\codex-findings\_index.json | ConvertFrom-Json
"count=$($idx.count) A=$($idx.samples.A.Count) B=$($idx.samples.B.Count) files=$($idx.files.Count)"
powershell -ExecutionPolicy Bypass -File .\eval\validate-findings.ps1 -Sample A
powershell -ExecutionPolicy Bypass -File .\eval\validate-findings.ps1 -Sample B
```

Expected:

```text
count=32 A=8 B=24 files=32
sample=A expected=8 missing=[] invalid=[]
ALL FINDINGS VALID
sample=B expected=24 missing=[] invalid=[]
ALL FINDINGS VALID
```

- [ ] **Step 4: Verify PowerShell scripts and whitespace**

Run from the worktree root:

```powershell
Get-ChildItem .\小马哥知识库 -Recurse -Filter *.ps1 | ForEach-Object {
  $errs = $null
  [void][System.Management.Automation.Language.Parser]::ParseFile($_.FullName, [ref]$null, [ref]$errs)
  "{0}: {1}" -f $_.Name, $(if($errs){"PARSE ERRORS=$($errs.Count)"}else{"ok"})
}
git diff --check
git status --short
```

Expected: every `.ps1` reports `ok`; `git diff --check` has no output; `git status --short` only shows intended files before the final commit.

- [ ] **Step 5: Commit any final missed-doc cleanup**

If Task 5 required edits, run:

```powershell
git add .\小马哥知识库\AGENT-OPS.md .\小马哥知识库\eval\claude-adjudication.md .\docs\superpowers\specs\2026-06-28-t7-quality-gate-design.md .\docs\superpowers\plans\2026-06-28-t7-quality-gate.md .\docs\superpowers\plans\2026-06-28-t7-env-fixes.md
git commit -m "docs(t7): remove stale quality-gate instructions"
```

If Task 5 required no edits, do not create a commit.

---

## Self-Review

- Spec coverage: all known stale areas are covered: `AGENT-OPS.md`, live Claude prompt, quality-gate design, quality-gate runbook, env-fixes plan, and `verdict-B.md`.
- Scope: no content wiki pages are modified. `wiki/aliases.md`, `raw/`, and individual findings JSON files are out of scope.
- Execution safety: every task has exact files, exact replacement text, verification commands, and a commit boundary.
- Known non-gating backlog is documented but not fixed here: article `444` two policy takes and article `91` one prediction take.
