# T7 环境/工具修复(P1–P7)实现计划 · 交付 Codex

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 brainstorming 复盘出的环境/工具问题(P1–P7)中可由 Codex 落地的部分,固化成确定性脚本 + 文档纪律,杜绝重踩。

**Architecture:** 全部在 worktree 内、PowerShell-only 执行,遵守 `小马哥知识库/AGENT-OPS.md`。可自动化的(编码、索引、样本范围)做成脚本/改文档并自验;环境级的(P1 Bash、P3 路径根)只文档化 + 列用户侧动作。每个 Task 自带验证命令与提交。

**Tech Stack:** Windows PowerShell 5.1、git、Codex(apply_patch 改文件 + PowerShell 跑命令)。

---

## Codex worker 约束(开工前必读)

1. **工作区**:`D:\cursor\小马哥时政历史\.worktrees\t7-quality-gate`,分支 `codex/t7-quality-gate`。所有路径相对此根;跨树操作用显式绝对路径。
2. **PowerShell-only**:本机 360安全卫士破坏 msys2 fork,**Bash 不可用**。所有命令用 PowerShell。改文件可用你自带的 apply_patch;跑/验证一律 PowerShell。
3. **编码纪律(P2)**:`.ps1` 注释只用 ASCII;`Get-Content` 读文本一律 `-Encoding UTF8`;写中文文件用 UTF-8(脚本带 BOM)。
4. **写入范围**:只动本计划点名的文件。**不得改** `raw/`、`wiki/`(除 §Task 4 在 `CLAUDE.md`/`AGENTS.md` 顶部加一行指针)、`wiki/aliases.md`、已有 `eval/codex-findings/*.json`。

## 已完成,勿重做
- P2:`eval/check-completeness.ps1`、`eval/validate-findings.ps1` 已 ASCII 化 + `-Encoding UTF8`。
- P5:`eval/codex-findings/_index.json` 已重生成为 A+B 累积。
- AGENT-OPS.md 已写并随 `0dc77ed` 提交。

## 文件结构

| 文件 | 职责 | Task |
|---|---|---|
| `小马哥知识库/eval/ensure-ps1-bom.ps1` | 全仓 .ps1 编码体检+加 BOM(确定性) | T1 |
| `小马哥知识库/eval/regen-index.ps1` | 从实际 findings 重建累积 _index | T2 |
| `小马哥知识库/eval/codex-brief.md` | 收尾改调 regen-index(P5)+ 限定单样本(P6) | T2/T3 |
| `小马哥知识库/CLAUDE.md` / `AGENTS.md` | 顶部加 AGENT-OPS 指针(P1/P3) | T4 |
| `小马哥知识库/eval/fix-encoding.ps1` | 单文件 GBK→UTF-8 一键转换助手(P7) | T5 |

---

## Task 1: P2 — 全仓 .ps1 UTF-8-with-BOM 体检与加固

PS 5.1 把无 BOM 的 .ps1 按 GBK 解析,中文(注释或 here-string)会吞行。统一保证含非 ASCII 的 .ps1 都带 BOM。

**Files:**
- Create: `小马哥知识库/eval/ensure-ps1-bom.ps1`

- [ ] **Step 1: 写体检/修复脚本**

Create `小马哥知识库/eval/ensure-ps1-bom.ps1`:

```powershell
# Ensure every .ps1 under the KB is UTF-8 with BOM (PS 5.1 mis-reads non-BOM UTF-8 as GBK).
# ASCII-only comments by design.
$ErrorActionPreference = 'Stop'
$kb = Split-Path -Parent $PSScriptRoot
$utf8strict = New-Object System.Text.UTF8Encoding($false, $true)
$utf8bom    = New-Object System.Text.UTF8Encoding($true)
$gb         = [System.Text.Encoding]::GetEncoding(54936)   # GB18030
$fixed = New-Object System.Collections.Generic.List[string]
$okc   = 0
foreach($f in (Get-ChildItem $kb -Recurse -Filter *.ps1 -File)){
  $bytes = [IO.File]::ReadAllBytes($f.FullName)
  $hasBom = ($bytes.Length -ge 3 -and $bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 0xBF)
  $hasNonAscii = $false
  foreach($b in $bytes){ if($b -gt 127){ $hasNonAscii = $true; break } }
  if((-not $hasNonAscii) -or $hasBom){ $okc++; continue }
  $isUtf8 = $true
  try { [void]$utf8strict.GetString($bytes) } catch { $isUtf8 = $false }
  $text = if($isUtf8){ $utf8strict.GetString($bytes) } else { $gb.GetString($bytes) }
  [IO.File]::WriteAllText($f.FullName, $text, $utf8bom)
  $fixed.Add(($f.FullName.Substring($kb.Length)) + $(if($isUtf8){' [utf8+bom]'}else{' [gbk->utf8+bom]'}))
}
"already-ok (ascii or already-bom): $okc"
"fixed: $($fixed.Count)"
$fixed | ForEach-Object { "  $_" }
```

- [ ] **Step 2: 运行体检/修复**

Run(worktree 的 `小马哥知识库/` 下):
```powershell
& .\eval\ensure-ps1-bom.ps1
```
Expected: 打印 `already-ok` 与 `fixed` 计数;`fixed` 列出含中文且原先无 BOM 的脚本(典型如 `\compile-batch.ps1`、`\normalize-corpus.ps1`)。纯 ASCII 脚本(check-completeness/validate-findings)计入 already-ok。

- [ ] **Step 3: 全仓 .ps1 语法解析自检(不执行)**

Run:
```powershell
Get-ChildItem .\小马哥知识库 -Recurse -Filter *.ps1 | ForEach-Object {
  $errs = $null
  [void][System.Management.Automation.Language.Parser]::ParseFile($_.FullName, [ref]$null, [ref]$errs)
  "{0}: {1}" -f $_.Name, $(if($errs){"PARSE ERRORS=$($errs.Count)"}else{"ok"})
}
```
Expected: 每个 .ps1 都 `ok`(加 BOM 不应破坏语法)。任一 `PARSE ERRORS` → 该文件原本可能是真 GBK 被误判,回 Step 1 检查该文件 `$isUtf8` 分支。

- [ ] **Step 4: 提交**

```powershell
git add 小马哥知识库/eval/ensure-ps1-bom.ps1 小马哥知识库/*.ps1
git commit -m "fix(p2): ensure non-ascii .ps1 are UTF-8 with BOM for PS5.1"
```

---

## Task 2: P5 — `regen-index.ps1` 助手 + 收尾改用它

`_index.json` 之前被单次运行覆盖。改成从实际 findings 重建(累积),并让 brief 收尾调用它而非手写。

**Files:**
- Create: `小马哥知识库/eval/regen-index.ps1`
- Modify: `小马哥知识库/eval/codex-brief.md`(收尾节)

- [ ] **Step 1: 写 regen-index.ps1**

Create `小马哥知识库/eval/regen-index.ps1`:

```powershell
# Rebuild eval/codex-findings/_index.json cumulatively from present findings + sample manifests.
# ASCII comments; UTF-8 reads.
$ErrorActionPreference = 'Stop'
$eval = $PSScriptRoot
$fd   = Join-Path $eval 'codex-findings'
function Read-Sample($name){
  Get-Content (Join-Path $eval $name) -Encoding UTF8 |
    Where-Object { $_ -match '^\s*\d' } |
    ForEach-Object { [int]([regex]::Match($_, '\d+').Value) }
}
$saA = @(Read-Sample 'sample-A.txt')
$saB = @(Read-Sample 'sample-B.txt')
$present = @(Get-ChildItem $fd -Filter *.json | Where-Object { $_.BaseName -match '^\d+$' } | ForEach-Object { [int]$_.BaseName })
$idx = [ordered]@{
  samples        = [ordered]@{ A = @($saA | Sort-Object); B = @($saB | Sort-Object) }
  count          = $present.Count
  files          = @($present | Sort-Object | ForEach-Object { "eval/codex-findings/$_.json" })
  regenerated_at = (Get-Date -Format 'yyyy-MM-ddTHH:mm:ssK')
}
$json = (($idx | ConvertTo-Json -Depth 5) -join "`n") -replace "`r`n", "`n"
[IO.File]::WriteAllText((Join-Path $fd '_index.json'), $json + "`n", (New-Object System.Text.UTF8Encoding($false)))
"index rebuilt: count=$($idx.count) A=$($saA.Count) B=$($saB.Count)"
```

- [ ] **Step 2: 运行并核对(不改变现有正确值)**

Run:
```powershell
& .\eval\regen-index.ps1
```
Expected: `index rebuilt: count=32 A=8 B=24`。

- [ ] **Step 3: 改 codex-brief.md 收尾,改调脚本**

确认 `小马哥知识库/eval/codex-brief.md` 的「## 收尾」第一条最终为:
```
- 全部样本处理完,不要手写 `_index.json`;必须运行 `eval/regen-index.ps1`,由脚本从现有 findings 和 sample manifests 重建累计索引。
```

- [ ] **Step 4: 验证替换并提交**

Run:
```powershell
Select-String -Path .\eval\codex-brief.md -Pattern 'regen-index.ps1' | Select-Object -First 1
git add 小马哥知识库/eval/regen-index.ps1 小马哥知识库/eval/codex-brief.md 小马哥知识库/eval/codex-findings/_index.json
git commit -m "fix(p5): cumulative _index via regen-index.ps1; brief calls it on finish"
```
Expected: Select-String 命中一行;提交成功。

---

## Task 3: P6 — codex-brief 限定单样本范围

worker 曾把样本 A+B 都跑了。明确限定每次只处理被指定的单一样本。

**Files:**
- Modify: `小马哥知识库/eval/codex-brief.md`(## 输入 节)

- [ ] **Step 1: 在「## 输入」加一条硬约束**

用 apply_patch 在 `小马哥知识库/eval/codex-brief.md` 的「## 输入」节末尾追加一行:
```
- **范围铁律**:本次只处理被指定的那一个样本文件(`sample-A.txt` 或 `sample-B.txt` 其一)里的 `<no>`,**不得**自行扩展到另一样本或样本外编号。
```

- [ ] **Step 2: 验证并提交**

Run:
```powershell
Select-String -Path .\eval\codex-brief.md -Pattern '范围铁律' | Select-Object -First 1
git add 小马哥知识库/eval/codex-brief.md
git commit -m "fix(p6): bound codex-brief to a single sample per run"
```
Expected: 命中一行;提交成功。

---

## Task 4: P1/P3 — 把 AGENT-OPS 纪律暴露给编译器 agent

`CLAUDE.md`/`AGENTS.md` 是编译器 agent 必读的规则层。在顶部加一行指针,确保它们也读到环境/路径纪律。

**Files:**
- Modify: `小马哥知识库/CLAUDE.md`、`小马哥知识库/AGENTS.md`(顶部)

- [ ] **Step 1: 两份文件顶部各加一行指针**

用 apply_patch 在 `小马哥知识库/CLAUDE.md` 与 `小马哥知识库/AGENTS.md` 的标题行(`# 小马哥时政历史知识库 ...`)正下方各插入一行:
```
> ⚙ 环境与工具纪律见 `AGENT-OPS.md`(PowerShell-only、UTF-8/GBK 编码、worktree 路径)。动手前先读。
```

- [ ] **Step 2: 验证并提交**

Run:
```powershell
Select-String -Path .\小马哥知识库\CLAUDE.md, .\小马哥知识库\AGENTS.md -Pattern 'AGENT-OPS.md'
git add 小马哥知识库/CLAUDE.md 小马哥知识库/AGENTS.md
git commit -m "docs(p1,p3): point CLAUDE/AGENTS to AGENT-OPS env+path discipline"
```
Expected: 两个文件各命中一行;提交成功。

---

## Task 5: P7 — `fix-encoding.ps1` 一键转码助手

worktree 内 `log.md` 已是 UTF-8、`.compile-log` 不在 worktree。但主树仍有 GBK 旧文件。提供幂等助手,供需要时对任意文件一键转码(默认不动主树)。

**Files:**
- Create: `小马哥知识库/eval/fix-encoding.ps1`

- [ ] **Step 1: 写转码助手**

Create `小马哥知识库/eval/fix-encoding.ps1`:

```powershell
# One-shot: convert a text file to UTF-8 (with BOM) if it is not already valid UTF-8.
# Usage: .\eval\fix-encoding.ps1 -Path <file>
# ASCII comments; idempotent.
param([Parameter(Mandatory=$true)][string]$Path)
$ErrorActionPreference = 'Stop'
if(-not (Test-Path -LiteralPath $Path)){ throw "not found: $Path" }
$resolvedPath = (Resolve-Path -LiteralPath $Path).ProviderPath
$bytes = [IO.File]::ReadAllBytes($resolvedPath)
$utf8strict = New-Object System.Text.UTF8Encoding($false, $true)
$isUtf8 = $true
try { [void]$utf8strict.GetString($bytes) } catch { $isUtf8 = $false }
if($isUtf8){ "already UTF-8, unchanged: $Path"; return }
$gb = [System.Text.Encoding]::GetEncoding(54936)
$utf8bom = New-Object System.Text.UTF8Encoding($true)
[IO.File]::WriteAllText($resolvedPath, $gb.GetString($bytes), $utf8bom)
"converted GBK -> UTF-8(BOM): $Path"
```

- [ ] **Step 2: 自测(对一个已 UTF-8 的文件应报 unchanged)**

Run:
```powershell
& .\eval\fix-encoding.ps1 -Path .\wiki\log.md
```
Expected: `already UTF-8, unchanged: ...\wiki\log.md`(幂等,不破坏已正确文件)。

- [ ] **Step 3: 提交**

```powershell
git add 小马哥知识库/eval/fix-encoding.ps1
git commit -m "feat(p7): idempotent GBK->UTF-8 helper for legacy files"
```

---

## Out of scope / 用户侧动作(非 Codex)

- **P1 根治**:在 360安全卫士里把 `C:\Program Files\Git` 加信任/白名单以恢复 Bash —— 需在 360 GUI 操作,Codex/agent 改不了。当前以 PowerShell-only 规避。
- **P3 结构性消除**:可选 `EnterWorktree` 把所有工具统一进 worktree;未验证,留作后续试验。
- **主树 `.compile-log` 转码**:主树是 gitignored 运行日志,且要"保持 main 干净";若需转,用户在主树自行 `小马哥知识库\eval\fix-encoding.ps1 -Path <主树 .compile-log>`。

## 自查(spec/问题覆盖)

- P2 → T1(全仓 .ps1 BOM 体检+解析自检)✓
- P5 → T2(regen-index.ps1 + brief 收尾改调用)✓
- P6 → T3(brief 范围铁律)✓
- P1/P3 → T4(CLAUDE/AGENTS 指针)+ Out-of-scope(360 白名单 / EnterWorktree)✓
- P7 → T5(fix-encoding 助手)+ Out-of-scope(主树 .compile-log 用户自跑)✓
- 全程 PowerShell-only、worktree 内、AGENT-OPS 纪律 → Codex 约束节 ✓
