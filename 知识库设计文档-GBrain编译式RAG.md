# 设计文档：小马哥时政历史知识库（GBrain 编译式 RAG）

Generated by /office-hours on 2026-06-26
Mode: Builder
Status: APPROVED

---

## 问题陈述

把 482 篇马永谙《小马哥财道》文稿（实际有正文约 435 篇，47 篇是空壳）编译成一个**可读、可查询、可问答**的知识库，自动抽取三类信息——**核心观点结论 / 事件（要素）/ 主体（国家·组织·人物）**——通过 GBrain 落地为一台完整的"大脑"。

参照素材：
- `编译式 RAG：LLM Wiki 原理详解.ipynb`（范式：三层 + 三操作）
- `编译式RAG第二节课：GBrain 核心功能详解.ipynb`（工具：self-wiring 建图 + 存储/检索/think）

## 这东西酷在哪

- 把一个财经历史播客的全部口语稿，变成一台能"问它**王安石为什么失败、金融战有哪些手法**"的大脑，跨 435 篇综合出**带原文出处**的答案。
- 不是黑箱向量检索，而是**人能打开读、能 `diff`、能审计**的互链 wiki——马永谙的每个论断都标着"这是他的 take，出自第 N 篇"。
- 一套 schema 同时服务三种用法：浏览互链 wiki、查知识图谱（谁参与了哪些事件、谁和谁对立）、智能问答。

## 约束

- **编译器 = Claude Code 或 Codex agent**（纯 agent，订阅驱动，**不走 API 脚本抽取**）。— 用户在 D4 明确选定
- **落地工具 = GBrain**（PGLite 本地引擎，单机零配置）。
- 语料是**中文口语稿**：噪声高（满是寒暄铺垫），且**无 `[[链接]]`**。
- 中国区网络：embedding 用 DashScope（注意国际/国内端点坑），think 用 DeepSeek。
- 工作盘不要放在 exFAT / 外置 SSD（PGLite 的 WASM + mmap 会冲突）。

## 前提（已在 D3 确认，无异议）

- **P1 · 必须有 LLM 编译这道**：裸文章没有 `[[链接]]`，纯 self-wiring 接不了线。这是"LLM 编译 + GBrain 建图"的混合，不是纯 Karpathy 范式。
- **P2 · slug 规范化是命门，无法 100% 自动**：王安石/安石/荆公、美联储/Fed 混用。词典 + LLM 覆盖大部分，长尾靠 `gbrain lint` 人工收口。**接受"先有少量重复、再合并"。**
- **P3 · 口语稿是噪声语料**：抽取要能滤掉寒暄/铺垫/重复，只留干货。pilot 第一件要验证的事。
- **P4 · 47 篇空壳排除**：没正文（如 #11 只有头部 + 录制时间），不进编译，`index.md` 标记"缺正文"。实际编译 ~435 篇。
- **P5 · 观点 = take，必须绑作者 + 出处**：这些是马永谙的**个人论断**，不是客观事实。必须标 `by: 马永谙` + `derived-from: sources/NNN`，否则知识库会把"一家之言"当"客观真相"。**这是这个语料最大的可信度风险。**
- **P6 · brain ⊥ source 纪律**：改了 wiki 页要重新 `gbrain import`，数据库不会自动同步。"重导入"是明确运维动作。

## 已考虑的方案

| 维度 | 备选 | 选定 |
|---|---|---|
| 用途范围（D1） | 智能问答 / 结构化资产 / **完整大脑** | **完整大脑**（wiki + 图谱 + think） |
| 抽取策略（D2） | 逐篇 / 实体中心一次到位 / **混合两遍** | **混合两遍**（全量抽取建图 + 重点节点编富页） |
| schema 范围（D3） | **5 实体类 + take + source** / 扩展 / 精简 | **5 实体类 + take + source** |
| 编译器引擎（D4） | API 脚本 / **纯 agent** / GraphRAG | **纯 agent**（Claude Code 或 Codex） |

被否决的关键备选：
- **GraphRAG 三元组直抽**——更自动但丢掉可读可审计的 markdown 中间层，违背编译式 RAG 核心价值，偏离 think 路线。
- **API 脚本抽取**——更可控更便宜，但用户要纯 agent 路线（贴两个 ipynb、工具链更干净、抽取这步免管 API key）。

---

## 推荐方案：完整管线

### 1. 三层目录骨架

```
小马哥知识库/
  raw/              # 482 篇原文（只读·源真相）。47 篇空壳标记排除
  wiki/             # 编译产物（agent 写，LLM 拥有）
    people/         # 人物页：wang-anshi.md, sima-guang.md, ma-yongan.md …
    orgs/           # 组织页：federal-reserve.md, alibaba.md, opec.md …
    countries/      # 国家/政权页：soviet-union.md, japan.md, bei-song.md …
    events/         # 事件页：plaza-accord-1985.md, xining-reform.md …
    takes/          # 观点页：financial-war-is-national-destiny.md …
    sources/        # 原文页：121-beisong-wanganshi.md（元数据 + 引用锚点）
    index.md        # 导航目录（含"缺正文"清单）
    log.md          # 变更日志
    aliases.md      # 规范名词典（slug 命门，逐批累积）
  CLAUDE.md         # 规则层（schema）— Claude Code 编译时遵守
  AGENTS.md         # 同内容（Codex 编译时遵守，保持 agent 无关）
  .git              # 版本控制：看 wiki 演进、可回滚
```

### 2. 规则层 `CLAUDE.md` / `AGENTS.md`（schema，最关键的一份文件）

必须写清六件事：
1. **6 种页面类型 + frontmatter 字段**（见下表）
2. **slug 规范化规则**：中文走拼音（王安石→`wang-anshi`、苏联→`soviet-union`、广场协议→`plaza-accord-1985`）；写链接前先查 `aliases.md`，命中别名落规范 slug；新实体写入 `aliases.md`
3. **关系动词清单**（见下）
4. **噪声过滤规则**：丢掉"大家好欢迎收听"这类寒暄、铺垫、口语重复，只抽干货
5. **观点纪律**：每个 take 必须 `by: 马永谙` + `derived-from: sources/NNN`
6. **铁律**：`raw/` 只读，所有写入只落 `wiki/`

**页面类型：**

| 类型 | slug 前缀 | 基元 | frontmatter 关键字段 |
|---|---|---|---|
| `person` 人物 | `people/` | entity | title, type, aliases[], role, era, sources[], updated |
| `org` 组织/机构/公司 | `orgs/` | entity | title, type, aliases[], kind(央行/企业/联盟), sources[], updated |
| `country` 国家/政权 | `countries/` | entity | title, type, aliases[], sources[], updated |
| `event` 事件 | `events/` | time | title, type, date/时间区间, actors[], cause, effect, sources[], updated |
| `take` 观点/论断 | `takes/` | take | title, type, claim(一句话), by:马永谙, topic, derived-from[], updated |
| `source` 原文 | `sources/` | media | title, type, article_no, 发布时间, 来源, updated |

**关系动词：** `mentions`提及(默认) · `participates-in`参与(主体→事件) · `about`论及(观点→主体/事件) · `opposes`对立(王安石↔司马光；中↔美) · `caused`导致(事件→事件) · `part-of`属于(人→国/组织) · `derived-from`出自(一切→`sources/NNN`) · `supports`/`contradicts`支持/反驳(观点↔观点)

### 3. Pass 1 — 逐篇抽取（agent，分批）

- **编排**：把 ~435 篇分批（每批 10-15 篇）。每批开一个 agent 会话（`claude -p` / `codex exec` headless，或交互式 TUI），喂入 `CLAUDE.md` 规则层 + 当前 `aliases.md` + 该批原文，让它写出 wiki 页 + 规范化 `[[链接]]`。
- **slug 一致性靠累积**：每批结束让 agent 更新 `aliases.md`（新实体规范 slug + 别名），下一批带上。这是让"王安石"在第 5 批和第 30 批落同一个 slug 的核心机制。
- **防爆上下文**：**绝不一次塞 435 篇**。分批 + 持久化 `aliases.md` = 既不爆上下文、又保 slug 收敛。
- **每篇都生成一个 `source` 页**，作为引用锚点（带发布时间，供时间线和 `derived-from`）。

### 4. 建图 — GBrain self-wiring（零 LLM）

```bash
gbrain init --pglite --no-embedding --force --path ./brain   # 先只建图
gbrain import wiki/                                            # 页面切块入库
gbrain extract links --source fs --dir wiki/                  # 纯正则解析 [[链接]] 建有向边
gbrain stats                                                  # 验收：pages/links 数
gbrain graph wang-anshi      # 看某节点连到哪
gbrain backlinks federal-reserve   # 反向看谁引用了它
```

### 5. Pass 2 — 重点节点编富页 + lint（agent，少量高价值）

- 用 `gbrain stats` / `backlinks` 找**高频节点**（被 ≥N 篇引用的实体、反复出现的观点）。
- agent 读这些节点的所有 backlink 原文，编出**跨源综合的富页面**（"读完王安石全部"的深度人物/事件/观点页）。
- `gbrain lint` 找断链/孤儿页/近义节点 → agent 合并近义 slug（P2 的人工收口在这步收）。

### 6. 第二半 — 向量 + think

```bash
# 重建带 embedding 的库（DashScope，维度必须显式给）
gbrain init --pglite --embedding-model <dashscope-model> --embedding-dimensions 1024 --force --path ./brain
gbrain import wiki/ && gbrain extract links --source fs --dir wiki/   # 重新导入 + 建图
# 算向量后即可：
gbrain think "马永谙认为金融战有哪些常用手法？" --model deepseek   # 跨页综合带引用
```
- **中国区坑**：DashScope 默认发往 `dashscope-intl` 国际端点会报 `invalid_api_key`，改国内端点。
- **建库 embedding 坑**：若 `~/.gbrain/config.json` 残留 `embedding_disabled: true`，`--embedding-model` 会被静默忽略 → 先改回 `false` 再 `init`。

### 7. 运维纪律

- 改了任何 wiki 页 → 重新 `gbrain import`（brain ⊥ source，不自动同步）。
- 新增文章 = 增量：`raw/` 放文件 → agent 抽取该篇 → `import` → `extract links`。
- `gbrain lint` 当作周期性体检。

---

## 成功标准

**Pilot（10-15 篇）：**
- 噪声过滤：抽出的页面里基本无寒暄/铺垫残留（人工抽检）
- slug 一致性：同一实体跨篇 slug 命中率（看有没有"王安石/安石"两个节点）
- 观点准确性：随机抽 5 个 take，`claim` 是否忠实于原文、`derived-from` 是否指对原文
- 成本：单篇平均 token/时长 → 推算全量成本与时间

**全量：**
- ~435 篇编译完成，`gbrain stats` 显示合理的 pages / links 规模
- `gbrain think` 在 5 个预设测试问题上给出**带正确引用**的答案
- 可回溯：随机抽 take/event，能 `derived-from` 一路找回原文 + 发布时间

---

## 下一步（含 Pilot 作业）

**Pilot 作业（这周就能做，是验证整套 schema 和 prompt 的关键）：**

选 10-15 篇**有跨文张力**的文章，跑通一小段闭环，亲眼验证三件事：(1) 噪声滤不滤得干净 (2) 同一实体 slug 一不一致 (3) 观点抽不抽得准。

建议选篇（两个有内部张力的主题簇）：
- 金融战簇：`137金融战` `138美元潮汐` `141人民币国际化必须建立在武力之上` `123美联储加息`
- 历史簇：`121王安石vs司马光` `122好的改革初心得不到好结果` `135奋六世之余烈` `120天行有常`

三步：
1. 搭三层骨架 + 手写 `CLAUDE.md`（schema 规则层）+ 放这 10-15 篇进 `raw/`
2. 跑 Claude Code agent 编译 → 打开 `wiki/` 看产物（人物/事件/观点页、互链、aliases.md）
3. `gbrain init --no-embedding` + `import` + `extract links` + `graph` → 看图谱成不成形

跑完这步，schema 和 prompt 才算被真实语料检验过，再决定全量。

**之后：** 可用 `/plan-eng-review` 把这份设计锁成带架构/测试/边界的实现计划。

---

## 开放问题

- **富页面"高频"阈值**：被 ≥3 篇还是 ≥5 篇引用才编富页？pilot 后用真实分布定。
- **观点去重粒度**：同一论断在多篇重复出现，合成一个 take 页（sources 累积）还是每篇一个？倾向**合成**，pilot 验证。
- **时间线视图**：GBrain 无原生时间线；事件页带 `date`，需要的话用 `sources` 的发布时间做后处理排序视图。
- **Claude Code vs Codex**：规则层保持 agent 无关（`CLAUDE.md` 与 `AGENTS.md` 同内容）；两者都能跑，教程用 Claude Code，建议主用它、Codex 作备份。
- **历史 vs 时政的实体边界**：王安石（历史人物）和美联储（现实机构）同在 `people`/`orgs`，schema 不分古今——确认这样可接受（倾向可接受，简单）。

---

## 我注意到你的思考方式

- 你一上来就把"**参照两个 ipynb**"和"**通过 gbrain 实现**"分开讲——你已经分清了"范式"和"工具"这两层，这是很多人会糊在一起的地方。
- D4 你没接受我推荐的"混合 A+B"，直接说"**我将纯依靠 claude code 或 codex agent 进行抽取**"——你对自己的工具链有判断，不为"性价比最优"的建议让步。这种"我知道我要什么"的明确，是能把项目推到底的人才有的。
- 你把抽取目标拆成"观点 / 事件 / 主体"，还在主体里细分"国家 / 组织 / 人物"——你是**带着结构来的**，不是"帮我处理一下这堆文件"。schema 设计因此几乎是顺着你给的轮廓长出来的。

---

## 工程评审结果（plan-eng-review · 2026-06-26）

> 本节是 `/plan-eng-review` 的交互评审产出，**在分歧处细化/覆盖上面的初稿**（管线改为两阶段、增量延后、质量验证改为人工触发 cross-model）。

### 评审决策（7 项）

| # | 议题 | 决策 |
|---|---|---|
| D2 | 实体一致性 | **两阶段**：Phase0 全量轻量抽实体 → 归并成**冻结 `aliases.md`** → Phase1 用冻结词典并行写页（消除"可并行 vs slug 一致"矛盾） |
| D3 | 批处理驱动 | **Shell 循环 + headless**（`claude -p` / `codex exec`），分批、记日志、**断点续跑**；claude/codex 通吃 |
| D4 | gbrain 版本 | **钉死教程版 0.42.x**（升级本机 0.41.29.0），参考文档与运行环境对齐 |
| D5 | 第二半 | **质量闸 + `ENABLE_SECOND_HALF` 开关**：pass-1 过线且开关开，才跑 pass-2 / embeddings / think |
| D6 | schema | **内置消歧规则 + 正反例 few-shot**（take vs fact、何时建实体页、事件粒度），保证 435 次独立调用分类一致 |
| D7 | 质量验证 | **Codex 识别 + Claude 复合交叉校验，人工触发**；固定同一批样本，跨 prompt 迭代可比 |
| D8 | 增量协议 | **记入 TODO，先不做**（存量静态，YAGNI） |

### 修订后的管线（权威顺序，覆盖初稿 §推荐方案 的执行次序）

```
0. 钉 gbrain 0.42.x → 搭三层骨架 → 写 CLAUDE.md（schema + 消歧规则 + few-shot + slug 规则）
1. Phase 0  ──headless 循环──▶ 全量「只列实体」 ──▶ 归并 ──▶ 冻结 aliases.md
2. Phase 1  ──headless 循环（按文章分片并行）──▶ 用冻结词典写 主体/事件/观点/source 页 + [[链接]]
3. 建图     gbrain import + extract links（--no-embedding）
4. 质量闸   固定样本：Codex 识别 + Claude 复合校验（人工触发）──过线？──否──▶ 调 schema/prompt 回到 1
            │过线
            ▼
5. [ENABLE_SECOND_HALF=true 才执行]
   pass-2 富页（高频节点）+ gbrain lint 合并近义 → 重建带 embedding 库 + import → gbrain think
```

### NOT in scope（明确不做，附理由）

- **增量协议**（新增节目并入）— 存量静态，YAGNI；记入 TODO（D8）
- **concept / policy / place 页类型** — D3 定为先不建，缺了再升级；当前可当 take 的 `topic` 标签
- **静态 gold set 基准** — 改用 D7 的人工触发 cross-model 校验（固定样本保可比）
- **时间线视图** — gbrain 无原生；事件页带 `date`，需要时后处理排序
- **团队 Postgres / 共享大脑** — 个人单机 PGLite 足够
- **think 成本优化** — 个人偶发使用，21× token 可接受
- **Minions dream-cycle 自动维护** — PGLite 跑不了常驻 worker；用人工触发 `lint` 替代

### What already exists（复用 vs 重造）

| 能力 | 来源 | 处置 |
|---|---|---|
| 存储 / 建图 / 检索 / think / lint | GBrain | **全复用**，不重造 ✓ |
| 编译器 + 质量校验器 | Claude Code / Codex | **复用**（订阅驱动）✓ |
| 命令链 + 踩坑指南 | 两个 ipynb | **直接参照** ✓ |
| schema 规则层 / aliases 词典 / headless 驱动脚本 / eval 触发器 | 自建（必要胶水） | 无现成，需写 |

结论：没有平行造轮子；自建部分都是范式要求的必要胶水。

### Failure modes（每条新 codepath 一个现实失败场景）

| codepath | 失败场景 | 有校验? | 有处理? | 用户可见? |
|---|---|---|---|---|
| Phase0 实体抽取 | 漏抽 / 错并 → 词典缺名或错并 | cross-model 校验（人工触发） | 词典可人工编辑 | 是（lint） |
| Phase1 写页 | slug 不匹配冻结词典 → 断链 | gbrain lint 断链检测 | lint 报告 → 人工/agent 修 | 是 |
| headless 批失败 | 某批崩 → 页缺失 | 驱动循环日志 | 跳过已完成、重试失败 | 是（日志） |
| gbrain import | `embedding_disabled` 残留 → 没算向量 | gbrain stats 看 Embedded | 教程坑A：改 false 重建 | 是（stats） |
| gbrain think | DashScope 国际端点 → invalid_api_key | — | 改国内端点 | 是（报错） |

**Critical gap 检查**：无"既无校验、又无处理、还静默"的失败 → **0 个 critical gap**（driver 日志 + lint + stats 让失败都可见）。

### 并行化策略

| 阶段 | 触及 | 依赖 |
|---|---|---|
| A · Phase0 实体扫描 | 全量原文（只读） | — |
| B · Phase1 写页 | `wiki/`（按文章分片） | A 的冻结 `aliases.md` |
| C · 建图 import+extract | brain | B 完成 |
| D · pass-2 + 向量 + think | `wiki/` + brain | 质量闸通过 + 开关开 |

执行顺序：`A → (B 按文章分片并行，可多 worktree) → C → 质量闸 → D`。**Phase 1 是唯一可并行的重活**；A/C/D 是串行关口。冲突风险低（B 各分片写不同 source 页，近义实体合并留到 C 后的 lint）。

### 验证图（测试覆盖类比）

```
管线阶段                      验证手段                          状态
[0] schema/骨架               自查：6 类型+动词+消歧例齐全        建前完成
[1] Phase0 实体→冻结词典      cross-model 抽检 slug 一致率        [质量闸·人工触发]
[2] Phase1 写页               gbrain lint 断链/孤儿              [→自动]
[3] 建图                      gbrain stats（pages/links 合理）   [→自动]
[4] 抽取忠实度（核心）         Codex 识别 + Claude 复合(固定样本)  [→EVAL·人工触发] ← 质量闸
[5] think 问答                5 个预设问题，人工核引用正确性      [→EVAL·人工触发]

覆盖：核心抽取质量有 cross-model EVAL；结构完整性有 lint + stats 自动兜底
```

### Implementation Tasks

- [ ] **T1 (P1, CC: ~15min)** — 环境 — 钉 gbrain 0.42.x（升级本机）+ 搭三层骨架 + `git init`
  - Surfaced by: 架构 D4
  - Verify: `gbrain --version` 显示 0.42.x
- [ ] **T2 (P1, CC: ~30min)** — schema — 写 `CLAUDE.md`/`AGENTS.md`：6 类型 + 关系动词 + slug 规则 + **消歧规则 + 3-5 正反例**
  - Surfaced by: 代码质量 D6
  - Verify: take/fact、建页与否、事件粒度都有明确 worked example
- [ ] **T3 (P1, CC: ~20min)** — 驱动 — headless 驱动脚本：分批、喂 schema+冻结词典+原文、记日志、断点续跑
  - Surfaced by: 架构 D3
  - Verify: 故意中断后重跑只处理未完成批次
- [ ] **T4 (P1, CC: ~15min)** — Phase0 — 「只列实体」抽取 prompt → 跑全量 → 归并 `aliases.md` 并冻结
  - Surfaced by: 架构 D2
  - Verify: aliases.md 覆盖样本篇目的人/国/组织，别名归并正确
- [ ] **T5 (P1, CC: ~20min)** — Phase1 — 写页 prompt（用冻结词典）→ 按文章分片并行跑
  - Surfaced by: 架构 D2
  - Verify: 同一实体跨篇 slug 一致；页带 frontmatter + `[[链接]]` + `derived-from`
- [ ] **T6 (P1, CC: ~10min)** — 建图 — `gbrain init --no-embedding` + import + extract links + graph/backlinks 抽查
  - Surfaced by: 管线
  - Verify: stats 的 pages/links 合理；backlinks 能追溯到 source
- [ ] **T7 (P1, CC: ~20min)** — 质量闸 — 固定样本 cross-model 校验触发器（Codex 识别 + Claude 复合）+ slug 一致率脚本
  - Surfaced by: 测试 D7 + 架构 D5
  - Verify: 输出可比的过线/未过线判定
- [ ] **T8 (P2, CC: ~25min)** — 第二半 — `ENABLE_SECOND_HALF` 开关 + pass-2 高频节点富页 + `gbrain lint` 合并近义
  - Surfaced by: 架构 D5
  - Verify: 开关 false 时第二半完全不跑
- [ ] **T9 (P2, CC: ~20min)** — 问答 — 带 embedding 重建库 + import + `gbrain think` 跑 5 个测试问题（注意 DashScope 国内端点）
  - Surfaced by: 管线 / 性能
  - Verify: think 答案带正确引用
- [ ] **T10 (P3)** — 增量 — 增量协议（更新词典→抽该篇→回填实体页→重 import）— **仅当持续追更才做**
  - Surfaced by: TODO D8

### 未决问题（沿用初稿开放问题，pilot 后定）
- 富页"高频"阈值：被 ≥3 还是 ≥5 篇引用？
- 观点去重粒度：同一论断合成一个 take 页 vs 每篇一个（倾向合成）

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | clean | 6 findings (4 架构 + 1 代码质量 + 1 测试) 全部当场拍板写回；1 TODO 延后；0 critical gap |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | 无 UI，n/a |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

- **VERDICT:** ENG CLEARED — 架构/代码质量/测试/性能四段走完，6 项发现全部当场决策并写回计划，0 个 critical gap。可进入实现（先跑 pilot）。
- **Outside Voice:** 未自动运行——本目录非 git 仓库（Codex outside-voice 需 git 根目录），且你在 D7 表达了"cross-model 人工触发"的偏好；可按需手动发起。

NO UNRESOLVED DECISIONS
